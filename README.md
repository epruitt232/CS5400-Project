# CS5400-Project

# Run the Program
python main.py

# Args
--agent
Description: The agent which is playing Solitaire.
Options: utlity | ? | ? | player

--load
Description: Option to load the last generated puzzle.
Options: omit to generate a new puzzle (will overwrite the last puzzle)